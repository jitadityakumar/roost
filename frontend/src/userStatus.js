// Shared between ListingsPage, ListingCard, and ListingDetail so the
// triage/approved/rejected labels can't silently drift out of sync.
export const USER_STATUS_LABEL = {
  triage: "Triage",
  approved: "Approved",
  rejected: "Rejected",
};
